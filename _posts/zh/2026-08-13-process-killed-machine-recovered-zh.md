---
audio: false
generated: true
image: false
lang: zh
layout: post
title: 进程终止，机器恢复
translated: true
type: note
---

进程已杀死，机器已恢复（可用内存恢复到51GB）。以下是完整情况：

## 发生了什么（半小时前）

**机器并非因程序错误而崩溃——而是内存耗尽导致死机。** 日志记录了整个过程：

| 时间 | 事件 |
| --- | --- |
| ~18:58 | `mixed_sample_2gb.txt`（2 GB）已创建；你对其运行了 `train_tokenizer.py`（`tok_train_sample.log` 在19:00创建） |
| 19:01:28 | `systemd-journald[747]: 内存压力下，正在刷新缓存` ← RAM 已耗尽 |
| 19:02:17 | **本次启动的最后一条日志——无正常关闭。** 系统挂起/死机 |
| 19:04:38 | 机器重启（启动列表确认：启动 `-1` 于19:02:17结束，启动 `0` 于19:04:38开始） |

没有 OOM-killer 或 panic 日志，因为机器在内核能够采取措施之前就已因内存颠簸而冻结（或者你强制重启了）。

## 刚刚复现——已确认

我使用内存监控重新运行了同样的命令，操作2 GB样本：

- **RSS 在8分钟内攀升至约35 GB**（581% CPU，多线程 Rust BPE）
- 系统可用内存在约90秒内从 **54 GB 降至 13.5 GB**（约600 MB/s，且速度还在加快）
- 我在此时终止了进程——按此趋势推算，它将耗尽全部62 GB RAM **外加33 GB swap**，然后像之前一样导致机器死机

## 根本原因

`ByteLevelBPETokenizer.train()` 会在 Rust 训练器中**将整个语料库保留在内存中**——包括65k词汇表的唯一单词和配对计数映射。内存放大倍数约为文件大小的 **15–20倍**：

- 2 GB 样本 → 约35+ GB（导致你的62 GB机器死机）
- 完整的 `mixed.txt`（11.5 GB）→ 约180 GB+——在这台机器上不可能完成

训练从未真正完成（没有写入 vocab.json/merges.txt——`tok_mixed/` 输出来自8月13日01:28的一次更早的不同运行）。

## 修复方案

1. **对语料库进行子采样**（推荐）——从 `mixed.txt` 中随机采样约100–200 MB进行训练。对于65k词汇表，这是标准做法，且具有足够的代表性；内存占用降至约3 GB。
2. **分块/采样行数**——流式处理行数据，只保留有限数量的行（效果与方案1相同，更简单）。
3. 更改 `--vocab-size` 无济于事——内存主要由唯一单词计数决定，而非词汇表大小。

需要我修改 `scripts/train_tokenizer.py` 以支持子采样/流式处理（例如添加 `--max-bytes` 或 `--sample-lines` 选项），然后安全地在2 GB文件上运行测试吗？
