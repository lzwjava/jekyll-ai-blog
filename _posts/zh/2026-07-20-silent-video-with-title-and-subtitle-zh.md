---
audio: false
generated: true
image: false
lang: zh
layout: post
title: 带标题和字幕的无声视频
translated: true
type: note
---

提交并推送至 `abe19a5`。变更内容如下：

**`ww gen-video <file>` 现在生成：**
- 总计15秒 —— 5张幻灯片 × 每张3秒
- **无音频** —— 静音视频
- **图片居中** 显示在画面中（垂直画布的中间60%区域）
- **标题** 位于顶部（粗体，白色字体搭配半透明黑色背景条）
- **副标题** 位于底部（自动换行，样式同上）
- 1080×1920 竖屏格式，针对抖音/微信视频号优化

**流程变更：**
- 大语言模型现在为每个场景生成 `标题` + `副标题` + `图片提示词`（无旁白脚本）
- 移除所有TTS代码（`say` 命令、音频转换、时长计算）
- 简化视频合成 —— 每张幻灯片固定为3秒，无音轨