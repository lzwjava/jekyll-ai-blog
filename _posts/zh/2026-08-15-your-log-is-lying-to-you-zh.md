---
audio: false
generated: true
image: false
lang: zh
layout: post
title: 你的日志在欺骗你
translated: true
type: note
---

整体情况如下：

## 训练一切正常——是你的日志在骗你

**过程并未卡住。** 此刻它正在活跃计算中（已通过 `py-spy` 确认：当前正位于 `train.py:331` 的 Triton kernel 内，GPU 利用率 100%，显存占用 8.7 GB，功耗 202 W）。

**实际进度已远超 iter 1900。** `out-bilingual/ckpt.pt` 中的检查点显示 `iter_num = 4500`，该文件上次写入时间为 **17:12:56**——我检查时大约是 3 分钟前。计算完全吻合：从 iter 1900（04:16）到 iter 4500（17:12）共 2600 次迭代，耗时 12.9 小时 = **17.9 秒/迭代**，与日志中的约 17.8 秒/迭代完全一致。

**日志/终端卡在 "iter 1900" 的原因：** `train.py` 使用了普通的 `print()`（未加 `flush=True`），且通过 `| tee` 输出的 stdout 属于块缓冲——日志和 tmux 面板只显示最近一次刷新（发生在 04:16）的约最后 8 KB 内容。此后缓冲的约 130 行日志会在进程退出时一次性全部输出。下次想要实时查看进度：`python3.12 -u train.py ...` 或 `PYTHONUNBUFFERED=1`。

## 预计完成时间：今天（8 月 15 日）19:40–19:45 HKT

- `max_iters = 5000`，当前约在 iter 4520（4500 已于 17:12:56 保存）
- 剩余约 500 次迭代 × 17.9 秒 ≈ **2.5 小时**（包含 5000 处的最终评估与检查点保存）
- 因此从现在（约 17:16）算起：大约在 **19:40–19:45** 完成，还需约 2.5 小时

当 tmux 面板显示 `step 5000: ...` 以及 `saving checkpoint to out-bilingual`，随后出现带有 `EXIT_CODE=0` 的 shell 提示符时，即表示训练已完成。与此同时，实时验证进度的方式是：无需 `python3.12 -c`——只需检查 `out-bilingual/ckpt.pt` 的修改时间；它每 250 次迭代更新一次（间隔约 74 分钟：下次约 18:27 保存 4750，最终约 19:42 保存 5000）。
