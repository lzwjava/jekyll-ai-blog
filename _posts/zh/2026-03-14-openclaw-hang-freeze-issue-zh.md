---
audio: false
generated: true
image: false
lang: zh
layout: post
title: OpenClaw 挂起导致系统冻结
translated: true
type: note
---

以下是您的系统可能挂起的原因以及日志揭示的内容总结：

### 1. 症状

* **系统软锁死：** 您的计算机变得无响应，仅显示黑屏/冻结屏幕上的鼠标图标。
* **服务失败：** SSH 登录失败，因为到端口 `18789` (OpenClaw) 的反向代理/转发拒绝连接。
* **恢复：** 需要手动硬重启，这大约在 **19:10 GMT+8** 成功使服务恢复在线。

### 2. 可能原因：OpenClaw 挂起

证据表明 **OpenClaw (gateway)** 先挂起，这可能导致系统其余部分或 GNOME 桌面环境资源耗尽：

* **SSH 日志：** 最早在 **17:34** 记录的错误显示端口 `18789` 已关闭，远早于完整的 GUI 冻结。
* **投递失败：** 重启后，OpenClaw 在其队列中发现一条消息 (`1b3ae44b...`) 已达到最大重试限制 (5/5)。这表明“poison pill”消息或 Telegram/消息投递尝试期间的网络挂起可能导致内部进程循环或阻塞。

### 3. 其他促成因素

* **网络/代理问题：** 服务如 `v2raya` 和 `PackageKit` 在启动时记录了大量到 `127.0.0.1:7890` 的连接失败，表明本地代理 (Clash/v2ray) 需要一段时间才能稳定，这可能导致其他服务出现超时相关的挂起。
* **NVIDIA 初始化：** 引导过程中出现瞬态错误，第二个 GPU 初始化失败，尽管这似乎不是中午冻结的主要原因。

### 4. 建议

* **如果再次发生：** 尝试按 `Ctrl+Alt+F3` 切换到文本控制台。如果能在那里登录，运行 `top` 查看是否有 `node` 进程占用 100% CPU。
* **专门重启 OpenClaw：** 如果丢失 UI 访问但仍能运行命令，使用 `openclaw gateway restart`。
* **检查“Failed”文件夹：** 我可以检查 `~/.openclaw/delivery-queue/failed/` 来查看确切卡住的消息，如果您想识别特定触发器。
