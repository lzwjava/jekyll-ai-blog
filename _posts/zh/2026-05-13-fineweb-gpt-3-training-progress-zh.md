---
audio: false
generated: true
image: false
lang: zh
layout: post
title: FineWeb GPT-3 训练进度
translated: true
type: note
---

## tmux 7 进度 — fineweb-gpt3 训练

**当前迭代约 7020 / 19073**（已完成 10B token 训练的约 37%）。

### 损失曲线（每 500 次迭代的验证损失）

| 步骤 | 训练 | 验证      |
| ---- | ---- | -------- |
| 4500 | 3.34  | 3.30     |
| 5000 | 3.31  | 3.26     |
| 5500 | 3.27  | 3.26     |
| 6000 | 3.23  | 3.25     |
| 6500 | 3.26  | **3.17** |
| 7000 | 3.22  | 3.18     |

健康的下降趋势。验证损失紧跟训练损失，无过拟合信号。从第 6000 步到第 6500 步，损失从 3.25 降至 3.17，是近期最大的一次单步跳跃——模型仍在学习有用结构，尚未进入平台期。

### 吞吐量
- **~11.11 秒/迭代** 稳态，每个评估周期峰值 **MFU ~12.86%**
- 评估步骤（每 500 次）耗时约 29 秒——额外的 18 秒用于评估加检查点保存
- 以 11.11 秒/迭代计算，剩余约 12,053 次迭代 → **约 37 小时** 到达 `max_iters=19073`

### 发现/观察
1. **训练稳定**。无损失尖峰、无 NaN、未见热节流。根据之前的 `nvidia-smi`，GPU 温度为 66C / 207W。
2. **每个评估窗口后 MFU 重置**（评估后立即降至 ~12.07%，再回升至 12.86%）。这仅是移动平均的预热效应——并非真正回退。
3. **已使用约 7.5 GB / 12 GB**——确认了之前的内存分析。`batch_size=4 → 6` 的余量仍然存在，但**不应在运行中途更改**，因为这会破坏学习率调度与优化器步数的关系。留到下一次运行再改。
4. **每 500 次迭代保存检查点**（`always_save_checkpoint=True`）。截至目前已保存约 14 次——若只保留一个文件，请确保 `out-fineweb-gpt3/` 不会填满磁盘（nanoGPT 会覆盖 `ckpt.pt`，所以应该没问题）。
5. **当前损失**：对于在 FineWeb-Edu 上完成 10B token 训练约 37% 的 124M 模型，~3.18 的验证损失是合理的。GPT-2 124M 在 OpenWebText 上完整训练结束时验证损失通常约为 2.85–2.90，因此仍有下降空间。

### TL;DR
运行状态健康，已完成约 37%，损失曲线正常，按当前速度还需约 37 小时。无需干预。