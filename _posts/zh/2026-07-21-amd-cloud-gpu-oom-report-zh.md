---
audio: false
generated: true
image: false
lang: zh
layout: post
title: AMD云GPU内存不足报告
translated: true
type: note
---

以下是全部内容的完整总结：

---

## AMD Radeon 云服务器 — 完整状态报告

### 服务器规格

| 组件 | 数值 |
| ----------- | ------- |
| **主机** | 36.150.116.206 |
| **SSH 端口** | 31154（当前宕机） |
| **操作系统** | Ubuntu 24.04.4 LTS |
| **CPU** | 2× AMD EPYC 9334（128线程） |
| **内存** | 503 GiB |
| **GPU** | AMD Radeon Graphics（gfx1100 / RDNA 3） |
| **显存** | 48 GiB |
| **ROCm** | 7.2.1，驱动 6.16.13 |
| **PyTorch** | 2.9.1+git（ROCm 7.2，位于 `/opt/venv`） |
| **磁盘** | `/` = 3.5TB 叠加层（2.8TB 空闲），`/workspace` = 98GB 回环（19GB 空闲） |

### GPU 计算测试

✅ **通过** — HIP向量加法（100万元素）计算正确，全部48GB显存可用，96个计算单元，时钟频率1760 MHz。

### 已完成操作

1. **诊断连接问题** — 发现SSH端口31094错误；实际SSH端口为31090、31098、31100（默认）、31154（我们的）
2. **将您的SSH密钥**添加到服务器
3. **下载 FLUX.2-dev**（166GB，39个文件）到 `/FLUX.2-dev` — **在252秒内成功完成**
4. **尝试推理** — 内存溢出导致实例崩溃。FLUX.2-dev 是一个 **320亿参数模型** — 仅Transformer部分就达60GB，即使启用CPU卸载也无法容纳在48GB显存中

### 当前状态

- **实例：已宕机** — 内存溢出导致端口31154上的SSH守护进程终止
- **模型文件**：166GB的FLUX.2-dev已安全保存在叠加文件系统的 `/FLUX.2-dev` 目录下
- **Jupyter Lab**：在端口8888上运行（令牌：`amd-oneclick`），但无法通过云代理访问
- **其他SSH端口**（31090、31098、31100）：仍在响应，但我们的密钥未获授权

### 后续步骤（重启后）

| 步骤 | 操作 | 原因 |
| ------ | -------- | ----- |
| 1 | 从云控制台重启实例 | 恢复端口31154上的SSH连接 |
| 2 | 删除FLUX.2-dev文件 | 释放166GB磁盘空间 |
| 3 | 下载 **FLUX.1-dev**（约35GB） | 120亿参数，FP16格式约需24GB显存 |
| 4 | 使用 `enable_sequential_cpu_offload()` 运行 | 可适配48GB显存 ✅ |

**FLUX.1-dev** 是您48GB GPU的正确选择——这是一个120亿参数模型，Transformer部分约需24GB显存，T5文本编码器约需10GB。通过CPU卸载，它将运行流畅。
