---
audio: false
generated: true
lang: zh
layout: post
title: 线性代数笔记
translated: true
type: note
---

## 示例

考虑二元变量 \\( x \\) 和 \\(y\\) 的二次型：

\\[ Q(x, y) = 2x^2 + 4xy + 3y^2 \\]

### 化为规范形的分步变换

1. **矩阵表示：**
   - 将二次型写成矩阵方程。与此二次型关联的矩阵 \\(A\\) 为：

\\[
A = \begin{pmatrix}
2 & 2 \\
2 & 3
\end{pmatrix}
\\]

   注意，非对角线元素是 \\(xy\\) 项系数的一半。

2. **求特征值和特征向量：**
   - 通过求解特征方程 \\( \det(A - \lambda I) = 0 \\) 来计算 \\(A\\) 的特征值。
   - 对每个特征值，找到对应的特征向量。

3. **对角化：**
   - 构造一个矩阵 \\(P\\)，其列是 \\(A\\) 的特征向量。
   - 计算 \\(D = P^TAP\\)，这将是一个对角矩阵，其对角线元素是 \\(A\\) 的特征值。

4. **变量替换：**
   - 定义新变量 \\(u\\) 和 \\(v\\)，使得：

\\[
\begin{pmatrix}
x \\
y
\end{pmatrix} = P \begin{pmatrix}
u \\
v
\end{pmatrix}
\\]

- 将这些代入原始二次型，得到关于 \\(u\\) 和 \\(v\\) 的新形式。

5. **规范形：**
   - 得到的二次型将处于规范形，即平方和的形式：

\\[
Q(u, v) = \lambda_1 u^2 + \lambda_2 v^2
\\]

   其中 \\(\lambda_1\\) 和 \\(\lambda_2\\) 是 \\(A\\) 的特征值。

### 解释

- 规范形揭示了二次型的几何性质。
- 如果两个特征值都为正，则该形式是正定的。
- 如果两者都为负，则是负定的。
- 如果符号不同，则该形式是不定的。

这个过程简化了二次型，使其性质更易于分析。

---

在二次型的语境中，术语“二次型的规范形”在英文中翻译为“canonical form of a quadratic form”。理解这个概念需要认识到如何通过线性代数技术将二次型简化或变换为标准形式。

### 二次型

二次型是多个变量的二次齐次多项式。例如，在变量 \\(x\\) 和 \\(y\\) 中，二次型可能看起来像：

\\[ Q(x, y) = ax^2 + bxy + cy^2 \\]

### 规范形

二次型的规范形是一个简化版本，揭示了基本性质，例如秩和符号差（正、负和零特征值的数量）。为了实现这种形式，我们通常通过对角化或其他正交变换进行变量替换。

#### 求规范形的步骤

1. **矩阵表示：** 将二次型表示为对称矩阵 \\(A\\)。对于上面的例子，矩阵将是：
\\[
A = \begin{pmatrix}
a & \frac{b}{2} \\
\frac{b}{2} & c
\end{pmatrix}
\\]

2. **对角化：** 找到一个正交矩阵 \\(P\\)，使得 \\(P^TAP\\) 是对角矩阵 \\(D\\)。这个过程涉及找到 \\(A\\) 的特征值和特征向量。

3. **变量替换：** 使用矩阵 \\(P\\) 来改变变量，将原始二次型变换为平方和，每一项对应一个特征值。

4. **规范形：** 得到的对角矩阵 \\(D\\) 表示二次型的规范形，其中每个对角线元素是 \\(A\\) 的一个特征值。

规范形有助于分析二次型的性质，例如确定它是正定、负定还是不定，这在优化和其他数学应用中至关重要。

---

**二次型的规范形** 指的是在应用适当的变量替换后，二次型的简化标准表示。这种变换使二次型的结构更清晰，更易于分析。

---

### **1. 二次型的定义**

在 \\( n \\) 个变量中的**二次型**是形式如下的函数：

\\[
Q(x) = x^T A x
\\]

其中：

- \\( x = (x_1, x_2, \dots, x_n)^T \\) 是一个 \\( n \\) 维列向量，
- \\( A \\) 是一个 \\( n \times n \\) 对称矩阵。

目标是将这个二次型变换为一个更简单的标准化形式。

---

### **2. 求规范形**

为了简化二次型，我们使用正交变量替换：

1. **求 \\( A \\) 的特征值和特征向量：**
    - 计算特征值 \\( \lambda_1, \lambda_2, \dots, \lambda_n \\)。
    - 找到一个标准正交的特征向量基。

2. **\\( A \\) 的对角化：**
    - 因为 \\( A \\) 是对称的，它可以被对角化为 \\( A = P D P^T \\)，其中：
      - \\( P \\) 是一个正交矩阵（其列是 \\( A \\) 的特征向量）。
      - \\( D \\) 是一个对角矩阵，对角线元素是特征值 \\( \lambda_1, \lambda_2, \dots, \lambda_n \\)。

3. **变量替换：**
    - 定义一个新变量 \\( y = P^T x \\)，那么：

\\[
Q(x) = x^T A x = (P y)^T A (P y) = y^T (P^T A P) y = y^T D y
\\]

    - 因为 \\( D \\) 是对角矩阵，二次型简化为：

\\[
Q(y) = \lambda_1 y_1^2 + \lambda_2 y_2^2 + \dots + \lambda_n y_n^2
\\]

    这就是**二次型的规范形**。

---

### **3. 规范形的解释**

- 规范形将二次型表示为按特征值加权的平方项之和。
- **特征值的符号**决定了二次型的性质：
  - 如果所有特征值都为正，则该形式是**正定的**。
  - 如果所有特征值都为负，则是**负定的**。
  - 如果符号混合，则是**不定的**。

这个过程在**圆锥曲线、优化和微分方程**中至关重要，因为它有助于对二次曲面和函数进行分类。

您想要一个更详细的例子吗？
