---
audio: false
generated: true
image: false
lang: zh
layout: post
title: 不要相信短信链接，使用应用
translated: true
type: note
---

问题：对于这条带有调查链接的京东配送短信，能否快速查看已送达包裹的照片？

答案：

简短版：我不能查看，而且那个链接也不可靠。

这里有几点需要注意：

**1. 我无法获取该照片。** URL 被截断了——它只到 `waybillCode=` 后面没有实际运单号。即使有完整的运单号，`h5-ql.jdl.com` 是一个京东物流 H5 页面，几乎肯定需要登录京东会话（cookies/token）才能显示签收照片。我的 `web_fetch` 无法携带你的认证信息，所以只会碰到登录拦截或空白状态。

**2. 默认将短信链接视为可疑。** 带有嵌入式链接的配送通知短信是中国最常见的钓鱼攻击手段之一（伪基站/钓鱼）。查看签收照片的正规方式：

- 打开 **京东 app** → 我的 → 我的快递/订单 → 找到该订单 → 物流详情。在“已放在家门口”的签收照片会原生显示在那里。
- 或者微信中的 **京东物流官方小程序**，通过运单尾号 `93941` 搜索。

不要直接点击短信中的链接。真正的京东配送照片始终可以通过已认证的 app 内部访问，这让短信链接显得多余。

**3. 电话号码** `<​IP_ADDRESS>`——我是说快递员号码 `139xxxx2377`——如果你确实需要确认，请拨打 app 内物流详情中列出的号码，而不是短信里的号码。

所以：是的，照片存在且可以快速查看——但请在京东 app 内查看，而不是通过那个链接，也不是通过我。