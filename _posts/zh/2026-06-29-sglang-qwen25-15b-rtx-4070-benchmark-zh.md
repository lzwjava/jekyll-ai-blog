---
audio: false
generated: true
image: false
lang: zh
layout: post
title: SGLang Qwen2.5-1.5B RTX 4070 基准测试
translated: true
type: note
---

SGLang 基准测试 — Qwen2.5-1.5B-Instruct 在 RTX 4070 上
======================================================

主题        TTFT(毫秒)   总耗时(毫秒)  令牌数   令牌/秒   质量
────────────────────────────────────────────────────────────────
数学        8453         9175        100      10.9     正常（截断）
编程        17           2196        300      136.6    包含函数
推理        17           497         66       132.8    正确
创意        17           125         15       119.6    俳句正常
事实        17           716         96       134.1    涵盖两者
翻译        17           147         18       122.2    正确（法语）
总结        17           373         49       131.3    正常

**主要发现：**

1. **TTFT（首令牌时间）：**
   - 第一次请求：**8.5秒** — 冷启动（CUDA 图形捕获、KV 缓存分配）。这只发生一次。
   - 预热后：**约17毫秒** — 基本瞬时。SGLang 的基数树 KV 缓存生效。

2. **吞吐量：** 预热后生成速度约 **120-137 令牌/秒**。对于 RTX 4070 上的 1.5B 模型，这还算不错 — 瓶颈在于小模型本身，而非框架。

3. **质量评估（1.5B 模型）：**
   - 数学：正确分解 17×23，但在完成前达到 100 令牌限制（答案 = 391）
   - 编程：生成一个带有文档字符串的真实函数，使用动态规划方法
   - 推理：得出正确答案（9 只羊），逐步推理正确
   - 创意：尚可的俳句 — "数据如河流 / 模型从模式中学习 / 智慧绽放"
   - 事实：TCP 与 UDP 的要点准确，术语使用得当
   - 翻译：完美的法语 — "Le renard brun rapide saute par-dessus le chien paresseux"
   - 总结：对相对论的 ELI5 解释扎实

4. **SGLang 的真正价值** 不在于单用户延迟 — 而在于并发吞吐量。1 个用户时约 130 令牌/秒；10 个并发用户时，SGLang 的连续批处理保持高吞吐量，而普通服务器则会卡死。另外，换用 Qwen2.5-7B 可获得明显更好的质量（使用约 8 GB 显存，仍能塞进你的 12 GB 显卡）。

要我测试更大的模型，或者将其设置为你的 Mac 可以访问的 OpenAI 兼容端点（例如，让 `ww` LLM 命令指向它）吗？
