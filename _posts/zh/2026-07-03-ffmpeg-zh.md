---
audio: false
generated: true
image: false
lang: zh
layout: post
title: FFmpeg
translated: true
type: note
---

以下是 `/home/xiaoxin/projects/FFmpeg` 中 FFmpeg 的完整构建摘要：

---

### 构建信息

| 项目 | 详情 |
| --- | --- |
| **版本** | `git-2026-07-02-14ed5e8`（来自 `master` 最新分支） |
| **编译器** | GCC 13（Ubuntu 13.3.0-6ubuntu2~24.04.1） |
| **汇编器** | NASM 2.16.01（用于手动优化的 x86 汇编） |
| **许可协议** | 非自由且不可再分发（因使用了 `--enable-nonfree`） |
| **架构** | x86-64，带优化汇编（已安装 `nasm`） |

### 配置选项

```
--enable-gpl --enable-version3 --enable-nonfree
```

未安装任何外部库的 `-dev` 包，因此 **未链接任何第三方编解码器**（x264、x265、libvpx、libmp3lame、libopus 等）。这是一个纯 FFmpeg 内置构建。

### 已构建的库（静态 `.a` 归档）

| 库 | 大小 | 用途 |
| --- | --- | --- |
| `libavcodec.a` | **162 MB** | 所有编解码器（构建主体） |
| `libavformat.a` | 51 MB | 容器封装/解封装 |
| `libavfilter.a` | 47 MB | 滤镜图引擎 |
| `libavutil.a` | 5.2 MB | 通用工具 |
| `libswscale.a` | 11 MB | 色彩空间/格式转换 |
| `libswresample.a` | 663 KB | 音频重采样 |
| `libavdevice.a` | 1.1 MB | 设备输入输出 |

### 可执行文件

| 可执行文件 | 大小 |
| --- | --- |
| `ffmpeg` | 29 MB |
| `ffprobe` | 29 MB |

### 功能计数

| 功能 | 数量 |
| --- | --- |
| 编码器 | ~189（69 音频，81 视频，14 字幕） |
| 解码器 | ~527 |
| 滤镜 | ~483 |
| 封装器 | ~191 |
| 解封装器 | ~368 |
| 比特流滤镜 | ~53 |
| 协议 | ~50 |
| 硬件加速 | 未检测到（无 VAAPI/VDPAU 开发头文件） |

### 构建统计

- **目标文件**：已编译 2,414 个 `.o` 文件
- **总构建大小**：约 1 GB（`.o` 文件中包含调试符号）
- **构建日期**：2026 年 7 月 3 日

### 缺失内容（无外部库）

由于未找到任何 `-dev` 包，以下功能 **未启用**：

- H.264 编码（需要 `libx264-dev`）
- H.265/HEVC 编码（需要 `libx265-dev`）
- VP8/VP9 编码（需要 `libvpx-dev`）
- MP3 编码（需要 `libmp3lame-dev`）
- Opus 编码（需要 `libopus-dev`）
- Vorbis 编码（需要 `libvorbis-dev`）
- ASS 字幕渲染（需要 `libass-dev`）
- VAAPI/VDPAU 硬件加速
- SDL2 输出（需要 `libsdl2-dev`）
- 字体渲染 / drawtext 滤镜（需要 `libfreetype-dev`）

### 快速使用说明

在不安装的情况下运行本地构建：

```
./ffmpeg -i input.mp4 output.avi
./ffprobe input.mp4
```

要启用外部库（x264、x265 等），安装相应的 `-dev` 包并重新运行 `./configure` + `make`。
