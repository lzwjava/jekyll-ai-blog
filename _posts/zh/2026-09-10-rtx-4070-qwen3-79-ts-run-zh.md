---
audio: false
generated: true
image: false
lang: zh
layout: post
title: RTX 4070 Qwen3 79 t/s 运行
translated: true
type: note
---

可以。已在 RTX 4070 上成功运行。

**结果：** 模型完全加载到 GPU，生成速度约 79 t/s。注意这是一个 Qwen3 *思考* 模型，因此默认会在答案前输出 `[Start thinking]` 块（我的 128 token 上限导致思考中途被截断）。

如需交互式聊天，请运行：

```bash
cd /mnt/data/llama.cpp
./build/bin/llama-cli -m models/Qwen3-8B-abliterated-q4_k_m.gguf -ngl 99 -c 8192 -cnv
```

几点说明：
- 此版本（b10894）已不存在 `-no-cnv` 参数。请使用 `-cnv` 开启对话模式；`-st` 为旧的单轮交互模式。
- 该工具无法保持交互式 TTY，因此我使用 `-p ... -st` 以非交互方式运行。
- 如需直接获取答案，可禁用思考功能：在提示词中输入 `/no_think`，或追加 `-sys "/no_think"`。
- 注意：`models/Qwen3.8-27B-Uncensored-draft-Q8_0.gguf` 大小为 3.1 GB，对于 27B Q8_0 模型来说过小（应为约 27 GB）。该文件名可能标记有误——在用作推测性草稿前请先验证。